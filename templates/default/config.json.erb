{
  "listen": {
    "host": "<%= @config[:listen] %>",
    "port": <%= @config[:port] %>
  },
  "new_relic": {
    "app_name": "<%= @config[:app_name] %>",
    "license_key": "<%= @config[:license_key] %>",
    "logging": "<%= @config[:logging] %>"
  }
}
